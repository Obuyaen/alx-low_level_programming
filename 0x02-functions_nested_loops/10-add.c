#include "main.h"
/**
 *add-adds two integers and returns result
 *@a:first number
 *@b:second number
 *Return:add the two numbers and print out the result
*/
int add(int a, int b)
{
return (a + b);

}
